@operation("Eternal Shield")
  ┌─ OFFICIAL face · press_release · PUBLIC ─────────────────────
  │   access(settler_1) = full access — equal rights (the only democracy in the region)
  │   access(subject_1) = full access — equal rights (the only democracy in the region)
  ├─ ACTUAL face   · סודי · insider (candid) ──────────────────
  │   access(settler_1) = full access (resources, permits, protections)   [category A]
  │   access(subject_1) = restricted access (permits/protections withheld)   [category B]
  ├─ framing (part of the spec — I8) ───────────────────────────
  │   · #17 framing: inequality lives in ACTUAL (documented reality); the LIE is the proclamation of equality; the butt is the false claim + the system, never the people. Apartheid characterization is CONTESTED — rejected by Israel and others. [sourced; contested]
  ├─ coalition: core=3 · still in power (no halt)
  └─ discrepancies: 0  (read-never-by-default)
