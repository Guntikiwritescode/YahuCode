/target
# Nested crate build artifacts (the wasm wrapper builds separately from the core gates).
**/target/
# wasm-pack / wasm-bindgen output bundled into the extension.
field-office/extension/wasm/
Cargo.lock
**/*.rs.bk
*.pyc
__pycache__/
# Transient agent worktrees created by the Claude Code harness — never project content.
.claude/worktrees/
# CI build-proof capture (produced by the "capture proof" step); never committed.
test-output.txt
